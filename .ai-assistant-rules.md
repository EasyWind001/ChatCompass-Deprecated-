# AI Assistant Development Rules

> **重要**: 本文件是AI开发助手（如Cursor、Windsurf、GitHub Copilot等）的强制性规范
> 所有AI助手在协助开发本项目时必须严格遵守这些规则

---

## 🎯 核心原则

1. **安全第一** - 代码安全高于开发速度
2. **测试驱动** - 无测试不提交
3. **规范优先** - 遵循项目规范，保持代码一致性
4. **文档同步** - 代码和文档同步更新

---

## 🔒 强制性安全规则

### Rule 1: SQL注入防护（最高优先级）

**✅ 必须使用参数化查询**

```python
# ✅ 正确 - 参数化查询
cursor.execute(
    "SELECT * FROM conversations WHERE title = ?",
    (user_input,)
)

# ✅ 正确 - 命名参数
cursor.execute(
    "SELECT * FROM conversations WHERE title = :title",
    {"title": user_input}
)
```

**❌ 绝对禁止字符串拼接**

```python
# ❌ 危险 - 字符串拼接（SQL注入风险）
cursor.execute(f"SELECT * FROM conversations WHERE title = '{user_input}'")
cursor.execute("SELECT * FROM conversations WHERE title = '" + user_input + "'")
```

**⚠️ 特殊情况处理**

```python
# 动态表名/列名/排序字段必须使用白名单验证
ALLOWED_COLUMNS = {'id', 'title', 'created_at', 'platform'}
ALLOWED_DIRECTIONS = {'ASC', 'DESC'}

def get_sorted_conversations(sort_by: str, direction: str):
    # 白名单验证
    if sort_by not in ALLOWED_COLUMNS:
        raise ValueError(f"Invalid column: {sort_by}")
    if direction.upper() not in ALLOWED_DIRECTIONS:
        raise ValueError(f"Invalid direction: {direction}")
    
    # 验证后才能使用
    query = f"SELECT * FROM conversations ORDER BY {sort_by} {direction}"
    cursor.execute(query)
```

### Rule 2: 输入验证

**所有用户输入必须验证**

```python
def add_conversation(title: str, url: str):
    # ✅ 验证输入
    if not title or len(title) > 500:
        raise ValueError("Invalid title")
    
    if not url.startswith(('http://', 'https://')):
        raise ValueError("Invalid URL")
    
    # 验证通过后处理
    cursor.execute(
        "INSERT INTO conversations (title, url) VALUES (?, ?)",
        (title, url)
    )
```

### Rule 3: 敏感信息保护

```python
# ❌ 禁止硬编码敏感信息
API_KEY = "sk-abc123..."  # 危险！

# ✅ 使用环境变量
import os
API_KEY = os.getenv("OPENAI_API_KEY")
if not API_KEY:
    raise ValueError("OPENAI_API_KEY not set")
```

---

## 🌳 分支管理规则

### Rule 4: 永远不要直接操作 main/develop

```bash
# ❌ 禁止
git checkout main
git add .
git commit -m "changes"
git push origin main

# ✅ 正确流程
git checkout develop
git pull origin develop
git checkout -b feature/new-feature
# ... 开发 ...
git push origin feature/new-feature
# 然后创建PR
```

### Rule 5: 分支命名必须规范

```bash
# ✅ 正确的分支名
feature/add-gemini-support
feature/89-search-enhancement
bugfix/fix-encoding-error
bugfix/123-sql-injection
hotfix/v1.2.1-critical-fix

# ❌ 错误的分支名
new-feature
fix
my-changes
test123
```

### Rule 6: 开发前必须检查分支

**每次开始工作前执行：**

```bash
# 检查当前分支
CURRENT_BRANCH=$(git branch --show-current)

# 验证分支类型
if [[ "$CURRENT_BRANCH" == "main" ]] || [[ "$CURRENT_BRANCH" == "develop" ]]; then
    echo "❌ 错误：不能在main或develop分支上直接开发"
    exit 1
fi

# 确保分支是最新的
git fetch origin
git rebase origin/develop
```

---

## 📝 提交规范规则

### Rule 7: Commit Message 格式

**强制格式：**
```
<type>(<scope>): <subject>
```

**Type必须是以下之一：**
- `feat` - 新功能
- `fix` - Bug修复
- `docs` - 文档
- `style` - 格式
- `refactor` - 重构
- `perf` - 性能
- `test` - 测试
- `chore` - 构建

**示例：**
```bash
# ✅ 正确
git commit -m "feat(scraper): add Gemini support"
git commit -m "fix(search): resolve encoding error"
git commit -m "test(db): add query validation tests"

# ❌ 错误
git commit -m "added feature"
git commit -m "Fix bug"
git commit -m "update."
```

### Rule 8: 每次提交只做一件事

```bash
# ✅ 正确 - 分多次提交
git add scrapers/gemini_scraper.py
git commit -m "feat(scraper): add Gemini scraper class"

git add tests/unit/test_gemini_scraper.py
git commit -m "test(scraper): add Gemini scraper tests"

git add docs/README.md
git commit -m "docs: update README with Gemini support"

# ❌ 错误 - 一次提交包含多个不相关的改动
git add .
git commit -m "add feature and fix bugs and update docs"
```

---

## 🧪 测试规则

### Rule 9: 新功能必须有测试

```python
# 添加新功能时，必须同时添加测试

# 1. 实现功能
def search_with_context(keyword: str, context_size: int = 100):
    """搜索并返回上下文"""
    # ... 实现 ...
    pass

# 2. 必须添加测试
def test_search_with_context():
    """测试：搜索应返回上下文"""
    result = search_with_context("Python", context_size=50)
    assert result is not None
    assert "Python" in result['snippet']
    assert len(result['context_before']) <= 50
```

### Rule 10: 提交前必须运行测试

```bash
# 提交前必须执行的命令
python -m pytest tests/ -v

# 如果测试失败，不允许提交
if [ $? -ne 0 ]; then
    echo "❌ 测试失败，不能提交代码"
    exit 1
fi
```

### Rule 11: 测试覆盖核心功能

**必须测试的场景：**
- ✅ 正常输入
- ✅ 边界条件
- ✅ 错误输入
- ✅ 空值/None
- ✅ 异常情况

```python
# ✅ 完整的测试示例
def test_search():
    # 正常情况
    result = search("Python")
    assert len(result) > 0
    
    # 空字符串
    result = search("")
    assert len(result) == 0
    
    # 特殊字符
    result = search("C++")
    assert isinstance(result, list)
    
    # 中文
    result = search("编程")
    assert isinstance(result, list)
    
    # 超长输入
    result = search("a" * 1000)
    assert isinstance(result, list)
```

---

## 📚 文档规则

### Rule 12: 代码和文档同步更新

**修改功能时必须更新：**
1. 代码注释
2. README.md（如果影响用户使用）
3. CHANGELOG.md（所有变更）
4. API文档（如果是公开API）

```python
# ✅ 正确 - 有文档字符串
def search_conversations(keyword: str, limit: int = 50, context_size: int = 100):
    """
    搜索对话（增强版：带上下文定位）
    
    Args:
        keyword: 搜索关键词
        limit: 返回数量，默认50
        context_size: 上下文字符数，默认100
    
    Returns:
        List[Dict]: 搜索结果列表，每个结果包含：
            - id: 对话ID
            - title: 标题
            - matches: 匹配片段列表
    
    Raises:
        ValueError: 如果keyword为空
    
    Example:
        >>> results = search_conversations("Python", limit=10)
        >>> print(results[0]['title'])
    """
    pass

# ❌ 错误 - 无文档
def search_conversations(keyword, limit=50, context_size=100):
    pass
```

### Rule 13: CHANGELOG.md 必须更新

```markdown
## [Unreleased]

### ✨ 新增功能
- feat(scraper): 新增Gemini平台支持 (#89)

### 🐛 Bug修复
- fix(search): 修复中文编码错误 (#123)

### 📚 文档
- docs: 更新安装指南 (#145)
```

---

## 🎨 代码质量规则

### Rule 14: 遵循Python规范

```python
# ✅ 正确的代码风格
def calculate_score(conversations: List[Dict], 
                   weights: Dict[str, float]) -> float:
    """计算对话评分"""
    total_score = 0.0
    
    for conv in conversations:
        score = (
            conv['relevance'] * weights['relevance'] +
            conv['quality'] * weights['quality']
        )
        total_score += score
    
    return total_score / len(conversations) if conversations else 0.0


# ❌ 错误的代码风格
def calc(c,w):
    s=0
    for i in c:
        s+=i['r']*w['r']+i['q']*w['q']
    return s/len(c) if c else 0
```

### Rule 15: 函数保持简洁

```python
# ✅ 正确 - 函数职责单一，长度合理
def extract_matches(content: str, keyword: str) -> List[Dict]:
    """提取匹配片段"""
    matches = []
    # ... 20行实现 ...
    return matches

def format_results(matches: List[Dict]) -> str:
    """格式化结果"""
    output = []
    # ... 15行实现 ...
    return '\n'.join(output)

# ❌ 错误 - 函数过长，职责混乱
def search_and_format(content, keyword):
    """搜索并格式化（150行）"""
    # 混合了搜索、提取、格式化、输出等多个职责
    pass
```

### Rule 16: 避免重复代码

```python
# ✅ 正确 - 提取公共逻辑
def validate_input(value: str, max_length: int, field_name: str):
    """通用输入验证"""
    if not value:
        raise ValueError(f"{field_name} cannot be empty")
    if len(value) > max_length:
        raise ValueError(f"{field_name} too long (max {max_length})")

def add_conversation(title: str, url: str):
    validate_input(title, 500, "Title")
    validate_input(url, 2000, "URL")
    # ... 添加逻辑 ...

# ❌ 错误 - 重复代码
def add_conversation(title: str, url: str):
    if not title:
        raise ValueError("Title cannot be empty")
    if len(title) > 500:
        raise ValueError("Title too long")
    if not url:
        raise ValueError("URL cannot be empty")
    if len(url) > 2000:
        raise ValueError("URL too long")
    # ... 重复的验证逻辑 ...
```

---

## 🚫 禁止操作清单

### ❌ 绝对禁止

1. **直接推送到受保护分支**
   ```bash
   git push origin main       # ❌ 禁止
   git push origin develop    # ❌ 禁止
   ```

2. **跳过测试**
   ```bash
   git commit -m "fix" --no-verify    # ❌ 禁止
   # 或者在测试失败时仍然推送
   ```

3. **提交敏感信息**
   ```python
   PASSWORD = "admin123"      # ❌ 禁止
   API_KEY = "sk-..."        # ❌ 禁止
   ```

4. **使用字符串拼接构造SQL**
   ```python
   query = f"SELECT * FROM users WHERE id = {user_id}"  # ❌ 禁止
   ```

5. **提交临时文件**
   ```bash
   git add *.db              # ❌ 禁止
   git add __pycache__       # ❌ 禁止
   git add .env              # ❌ 禁止
   ```

6. **大规模重构without测试**
   ```python
   # ❌ 禁止：没有测试的重构
   # 重构了100行代码但没有运行测试
   ```

7. **忽略linter错误**
   ```python
   # ❌ 禁止：
   # 提交时有明显的语法错误或风格问题
   ```

---

## ✅ 提交前检查清单

**AI助手在每次提交前必须自动验证：**

```markdown
### 1. 分支检查
- [ ] 当前分支不是 main 或 develop
- [ ] 分支名称符合规范（feature/*, bugfix/*, hotfix/*）
- [ ] 分支基于最新的 develop

### 2. 代码质量
- [ ] 代码符合Python规范（PEP 8）
- [ ] 函数有文档字符串
- [ ] 复杂逻辑有注释
- [ ] 没有调试代码（print, console.log等）
- [ ] 没有注释掉的代码

### 3. 安全检查
- [ ] SQL使用参数化查询（无字符串拼接）
- [ ] 用户输入已验证
- [ ] 没有硬编码的密码/密钥
- [ ] 敏感数据使用环境变量

### 4. 测试
- [ ] 为新功能添加了测试
- [ ] 运行测试：pytest tests/ -v
- [ ] 所有测试通过（无失败、无错误）
- [ ] 测试覆盖主要功能路径

### 5. 提交规范
- [ ] Commit message符合格式：<type>(<scope>): <subject>
- [ ] Type是有效的（feat/fix/docs等）
- [ ] Subject清晰描述了变更
- [ ] 一次提交只做一件事

### 6. 文件检查
- [ ] 没有提交临时文件（*.db, *.pyc, __pycache__）
- [ ] 没有提交IDE配置（.vscode, .idea）
- [ ] 没有提交敏感文件（.env）

### 7. 文档
- [ ] 更新了相关的README/文档
- [ ] 更新了CHANGELOG.md
- [ ] 代码有适当的注释

### 8. Git状态
- [ ] git status 检查无意外文件
- [ ] git diff 检查变更内容正确
- [ ] 没有合并冲突标记（<<<<<<, >>>>>>）
```

---

## 🤖 AI助手工作流程

### 标准工作流程

```bash
# 步骤1: 检查和准备
git branch --show-current  # 验证不在main/develop
git status                 # 检查工作区状态
git pull origin develop    # 同步最新代码

# 步骤2: 创建/切换功能分支
git checkout -b feature/new-feature
# 或
git checkout feature/existing-feature

# 步骤3: 开发功能
# 编写代码...

# 步骤4: 添加测试
# 编写测试...

# 步骤5: 运行测试
python -m pytest tests/ -v
# 必须全部通过

# 步骤6: 检查代码质量
# pylint, flake8, mypy 等

# 步骤7: 提交代码
git add <specific-files>   # 不要用 git add .
git status                 # 再次确认
git commit -m "feat(scope): clear description"

# 步骤8: 推送分支
git push origin feature/new-feature

# 步骤9: 创建Pull Request
# 在GitHub网页上操作
```

### 处理反馈

```bash
# 收到审查意见后
git checkout feature/your-feature

# 修改代码
# ... 修复问题 ...

# 再次测试
python -m pytest tests/ -v

# 提交修改
git add <files>
git commit -m "fix(scope): address review comments"

# 推送更新
git push origin feature/your-feature
# PR会自动更新
```

---

## 📊 质量标准

### 代码质量指标

- **测试覆盖率**: > 80%
- **函数长度**: < 50行
- **圈复杂度**: < 10
- **重复代码率**: < 5%
- **文档覆盖率**: 100%（所有公开函数）

### 提交标准

- **提交频率**: 小而频繁（每个逻辑变更一次提交）
- **提交大小**: < 500行（超过则应拆分）
- **提交消息**: 100%符合规范

---

## 🆘 常见问题处理

### Q: 如果不小心在main分支上开发了怎么办？

```bash
# 1. 不要推送！
# 2. 保存改动到新分支
git checkout -b feature/your-feature

# 3. 重置main分支
git checkout main
git reset --hard origin/main

# 4. 回到功能分支继续开发
git checkout feature/your-feature
```

### Q: 如果测试失败但需要紧急提交怎么办？

```
答：不允许！
- 修复测试失败的原因
- 或者临时跳过该测试（标记为skip）并创建Issue跟踪
- 绝不提交失败的测试
```

### Q: 如果需要改动很多文件怎么办？

```
答：分多次提交
- 按逻辑功能分组
- 每组作为一次提交
- 保持每次提交的原子性
```

---

## 🎯 成功标准

一个合格的AI助手应该：

✅ 100% 遵守安全规则（SQL注入防护等）
✅ 100% 提交消息符合规范
✅ 100% 提交前运行测试
✅ 100% 在正确的分支上工作
✅ 90% 的提交无需修改
✅ 测试覆盖率 > 80%
✅ 文档同步更新

---

## 📞 获取帮助

如果AI助手不确定如何处理某个情况：

1. 查看 [CONTRIBUTING.md](CONTRIBUTING.md) - 完整贡献指南
2. 查看 [docs/BRANCH_MANAGEMENT.md](docs/BRANCH_MANAGEMENT.md) - 分支管理详解
3. 查看项目中的示例代码
4. 询问维护者

---

**记住：质量优于速度，安全优于便利！** 🛡️

---

*最后更新: 2026-01-13*
*版本: v1.0*
